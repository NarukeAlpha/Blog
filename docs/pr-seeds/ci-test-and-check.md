# CI Test And Check

This placeholder branch exists to open the PR and reserve the CI workstream.

Planned focus:
- add GitHub Actions for install, test, and check runs
- make pull requests validate the current repo scripts automatically
- create consistent feedback before broader refactors and feature work
